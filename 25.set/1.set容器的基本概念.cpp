


/*  set容器也可以叫作集合容器
  特点：
    所有元素都会在插入时被自动排序
    使用set或者multiset都只需要包含set的头文件即可
  本质：
    set/multiset属于关联式容器，底层结构是用二叉树实现
  set和multist的区别：
    set不允许容器中有重复的元素
    multist允许容器中有重复的元素

    set只允许insert()插入数据
*/